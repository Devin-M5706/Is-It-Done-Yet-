# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/

# self-signed TLS material — regenerated on first run, never commit
cert.pem
key.pem

# captured personal accelerometer data + trained model (regenerate locally)
data/model.joblib

# claude-code-eyes captured frames + local camera config
.claude-code-eyes/
.cce.env

# OS / editor
.DS_Store
.vscode/
.idea/

# captured data + trained artifacts (retrain.py)
data/raw/*
!data/raw/.gitkeep
data/model.joblib
data/RETRAIN_REPORT.md
